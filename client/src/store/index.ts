import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

import record from "./record";

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    record,
  },
});
